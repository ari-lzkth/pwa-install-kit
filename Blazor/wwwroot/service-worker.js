// In der Entwicklung absichtlich leer / ohne Caching,
// damit Änderungen sofort sichtbar sind.
// Der "echte" Service Worker ist service-worker.published.js (nur im Release-Build aktiv).
self.addEventListener('fetch', () => { });
